# Examples / Примеры

Эта директория содержит примеры стратегий и скрипты для их запуска.

## Night Cat Samurai Strategy

**Файлы**:
- `night_cat_samurai_strategy.json` — конфигурация стратегии "ETH-5m ОХОТА НОЧНОГО КОТА САМУРАЯ"
- `run_night_cat_samurai.py` — скрипт для запуска бэктеста

### Описание стратегии

Индикаторная стратегия с следующими характеристиками:

- **Индикаторы**: RSI, Bollinger Bands, Volume MA
- **Временной фильтр**: торговля только с 00:00 до 07:00 UTC
- **Вход в лонг**: RSI < 22 AND low <= нижняя полоса BB AND volume > vol_ma * 0.5
- **Вход в шорт**: RSI > 80 AND high >= верхняя полоса BB AND volume > vol_ma * 0.5
- **Пирамидинг**: до 4 позиций
- **Мартингейл**: множитель 1.5 (размер позиции растет как 1, 1.5, 2.25, 3.375...)
- **TP/SL**: 2% для лонгов и шортов

### Как запустить

1. Подготовьте данные:
   ```bash
   # Создайте директорию для данных
   mkdir -p data

   # Поместите туда CSV файл с данными ETHUSDT 5m
   # Формат: timestamp,open,high,low,close,volume
   ```

2. Запустите бэктест:
   ```bash
   python examples/run_night_cat_samurai.py
   ```

3. Результат будет выведен в консоль:
   ```
   Loading strategy: eth_5m_night_cat_samurai
   Symbol: ETHUSDT
   Pyramiding: 4
   Martingale multiplier: 1.5

   Running backtest...

   ============================================================
   BACKTEST RESULTS
   ============================================================
   Final equity:     $12,345.67
   Total return:     23.46%
   Max drawdown:     -8.92%
   Number of trades: 42
   ...
   ```

### Адаптация для других стратегий

Вы можете использовать этот пример как шаблон:

1. Скопируйте `night_cat_samurai_strategy.json` → `my_strategy.json`
2. Измените параметры:
   - `rsi_len`, `bb_len`, `bb_mult`, `vol_ma_len`, `vol_mult`
   - `pyramiding`, `martingale_mult`
   - `tp_long_pct`, `sl_long_pct`, etc.
3. Измените правила в секции `rules`:
   - `long_entry`, `short_entry`
   - `long_exit`, `short_exit` (если нужны правила выхода помимо TP/SL)
4. Обновите `run_night_cat_samurai.py` для загрузки вашего конфига

### Дополнительная документация

См. [STRATEGIES.md](../STRATEGIES.md) для полного руководства по созданию индикаторных стратегий.

---

## Live/Paper Trading CLI

**Новая возможность**: Запуск стратегий в режиме реального времени (live/paper trading)

### Что это?

`llm-trading-live` — CLI-утилита для запуска торговли в реальном времени:
- **Paper trading** — симуляция торговли без реальных денег (безопасно для тестирования)
- **Live trading** — реальная торговля на бирже Binance (требует API ключей и подтверждения)

Поддерживает:
- Любую индикаторную стратегию из JSON
- Опциональную LLM-обертку для режимного фильтра
- Автоматическое получение данных с биржи
- Безопасные проверки перед запуском live-режима

### Quick Start: Paper Trading

1. **Убедитесь, что в .env установлено**:
   ```bash
   EXCHANGE_TYPE=paper
   PAPER_INITIAL_BALANCE=10000.0
   PAPER_FEE_RATE=0.0005
   ```

2. **Запустите paper trading**:
   ```bash
   # Установите пакет (если еще не установлен)
   pip install -e .

   # Запустите с Night Cat Samurai стратегией
   llm-trading-live \
     --mode paper \
     --strategy-config examples/night_cat_samurai_strategy.json \
     --symbol ETHUSDT \
     --timeframe 5m \
     --initial-equity 10000
   ```

3. **Остановка**: Нажмите `Ctrl+C` для graceful shutdown

### Paper Trading с LLM-фильтром

Добавляет режимный анализ через LLM для масштабирования сигналов:

```bash
# С использованием Ollama (локальный LLM)
llm-trading-live \
  --mode paper \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --symbol ETHUSDT \
  --timeframe 5m \
  --llm-enabled \
  --llm-model llama3.2 \
  --llm-provider ollama \
  --horizon-bars 48 \
  --k-max 2.0

# С использованием OpenAI
llm-trading-live \
  --mode paper \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --llm-enabled \
  --llm-model gpt-4 \
  --llm-provider openai
```

**Как работает LLM-фильтр**:
- Периодически (каждые `horizon-bars` баров) LLM анализирует рынок
- Определяет режим: бычий (bullish) / медвежий (bearish) / нейтральный
- Масштабирует сигналы стратегии:
  - В бычьем режиме: long позиции усиливаются, short ослабляются
  - В медвежьем режиме: short позиции усиливаются, long ослабляются
  - В нейтральном режиме: обе стороны ослабляются

### Live Trading (РЕАЛЬНЫЕ ДЕНЬГИ) ⚠️

**⚠️ ВНИМАНИЕ**: Live trading торгует реальными деньгами! Используйте с осторожностью!

**Предварительные требования**:
1. Аккаунт на Binance
2. API ключи (получить на https://www.binance.com/en/my/settings/api-management)
3. Понимание рисков торговли

**Настройка .env для live trading**:
```bash
# Тип биржи
EXCHANGE_TYPE=binance

# ОБЯЗАТЕЛЬНО: подтверждение live торговли
EXCHANGE_LIVE_ENABLED=true

# Binance API ключи
BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# ВАЖНО: Сначала протестируйте на testnet!
BINANCE_TESTNET=true  # true = testnet (безопасно), false = mainnet (РЕАЛЬНЫЕ ДЕНЬГИ)

# Торговая пара и параметры
BINANCE_TRADING_SYMBOL=BTC/USDT
BINANCE_LEVERAGE=1
BINANCE_MIN_NOTIONAL=10.0
```

**Запуск live trading**:
```bash
# Шаг 1: ВСЕГДА начинайте с testnet!
# Убедитесь: BINANCE_TESTNET=true в .env
llm-trading-live \
  --mode live \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --yes-i-know-this-is-real-money

# Шаг 2: Только после успешных тестов на testnet - mainnet
# Установите: BINANCE_TESTNET=false в .env
# ВНИМАНИЕ: Это будет использовать реальные деньги!
llm-trading-live \
  --mode live \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --yes-i-know-this-is-real-money
```

**Проверки безопасности**:
- Требуется флаг `--yes-i-know-this-is-real-money`
- Требуется `EXCHANGE_LIVE_ENABLED=true` в .env
- Требуется `EXCHANGE_TYPE=binance` в .env
- CLI показывает предупреждения о testnet/mainnet режиме
- Graceful shutdown при Ctrl+C (закрывает позиции корректно)

### Параметры CLI

**Основные параметры**:
- `--mode` — `paper` или `live`
- `--strategy-config` — путь к JSON файлу стратегии (обязательно)
- `--symbol` — торговая пара (по умолчанию из конфига)
- `--timeframe` — таймфрейм баров: `1m`, `5m`, `15m`, `1h`, и т.д. (по умолчанию: `5m`)
- `--initial-equity` — начальный капитал для paper trading (по умолчанию: 10000)
- `--fee-rate` — комиссия за сделку (по умолчанию: 0.0005 = 0.05%)

**LLM параметры**:
- `--llm-enabled` — включить LLM-фильтр
- `--llm-model` — модель LLM (по умолчанию: `llama3.2`)
- `--llm-provider` — провайдер: `ollama` или `openai` (по умолчанию: `ollama`)
- `--horizon-bars` — интервал обновления LLM в барах (по умолчанию: 48)
- `--k-max` — максимальный множитель позиции от LLM (по умолчанию: 2.0)
- `--temperature` — температура LLM (по умолчанию: 0.1)

**Параметры движка**:
- `--poll-interval` — интервал опроса биржи в секундах (по умолчанию: 1.0)
- `--log-level` — уровень логирования: `DEBUG`, `INFO`, `WARNING`, `ERROR`

**Безопасность**:
- `--yes-i-know-this-is-real-money` — обязательный флаг для live-режима

### Примеры команд

```bash
# 1. Простой paper trading
llm-trading-live --mode paper --strategy-config examples/night_cat_samurai_strategy.json

# 2. Paper trading с кастомными параметрами
llm-trading-live \
  --mode paper \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --symbol BTCUSDT \
  --timeframe 15m \
  --initial-equity 50000 \
  --fee-rate 0.001

# 3. Paper trading с LLM (Ollama)
llm-trading-live \
  --mode paper \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --llm-enabled \
  --horizon-bars 24 \
  --k-max 3.0

# 4. Live trading на testnet (безопасно)
# Требует: BINANCE_TESTNET=true в .env
llm-trading-live \
  --mode live \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --yes-i-know-this-is-real-money

# 5. Live trading с LLM на testnet
llm-trading-live \
  --mode live \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --llm-enabled \
  --llm-model gpt-4 \
  --llm-provider openai \
  --yes-i-know-this-is-real-money

# 6. Debug режим с подробным логированием
llm-trading-live \
  --mode paper \
  --strategy-config examples/night_cat_samurai_strategy.json \
  --log-level DEBUG
```

### Мониторинг и логи

CLI выводит:
- **Startup banner** с конфигурацией
- **Real-time updates** при каждом новом баре
- **Order execution logs** при размещении ордеров
- **LLM regime updates** (если включено)
- **Shutdown summary** с итоговой статистикой

Пример вывода:
```
======================================================================
🚀 LLM Trading System - Live Trading CLI
======================================================================
Mode:       PAPER
Symbol:     ETHUSDT
Timeframe:  5m
Strategy:   examples/night_cat_samurai_strategy.json
LLM:        Enabled
  Model:    llama3.2
  Horizon:  48 bars
Equity:     $10,000.00
Base Size:  10.00%
======================================================================

[12:34:56] 🚀 Trading started - Press Ctrl+C to stop

[12:35:01] 📊 New bar: ETHUSDT 2024-11-18 12:35:00 | O: 2050.5 H: 2052.3 L: 2049.8 C: 2051.2
[12:35:01] 📈 LLM regime updated: bullish | prob_bull=0.750 prob_bear=0.250 | k_long=1.500 k_short=0.500
[12:35:01] 📤 Order: LONG 0.15 ETH @ 2051.2 (scaled from 0.10)
[12:40:01] 📊 New bar: ETHUSDT 2024-11-18 12:40:00 | O: 2051.2 H: 2053.0 L: 2050.5 C: 2052.8
...

^C
[12:45:30] 🛑 Shutting down gracefully...

======================================================================
📊 Trading Session Summary
======================================================================
Duration:        0.17 hours
Bars Processed:  3
Orders Executed: 1
Trades Closed:   0

Initial Equity:  $10,000.00
Final Equity:    $10,045.23
P&L:             +$45.23 (+0.45%)
======================================================================
```

### Рекомендации

**Для начинающих**:
1. Начните с **paper trading** — это безопасно и бесплатно
2. Тестируйте стратегию минимум 1-2 недели на paper trading
3. Только после успешных тестов переходите на **testnet**
4. Только после успешных тестов на testnet переходите на **mainnet** (реальные деньги)

**Для продвинутых**:
- Используйте `--log-level DEBUG` для отладки
- Комбинируйте LLM с проверенными индикаторными стратегиями
- Мониторьте логи и equity curve в реальном времени
- Используйте низкий `--initial-equity` для первых live-сессий
- Всегда держите stop-loss в стратегии

**Безопасность**:
- Никогда не коммитьте API ключи в git
- Используйте API ключи с ограниченными правами (только spot/futures trading)
- Включите IP whitelist на Binance для API ключей
- Регулярно проверяйте баланс и открытые позиции
- Тестируйте новые стратегии только на testnet/paper

### Помощь

Для полного списка параметров:
```bash
llm-trading-live --help
```

Документация по стратегиям: [STRATEGIES.md](../STRATEGIES.md)
