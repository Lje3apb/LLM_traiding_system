# 🔍 Анализ использования RSI параметров в стратегии

**Стратегия:** `night_cat_samurai_strategy`
**Вопрос:** Влияют ли параметры `rsi_ovs` и `rsi_ovb` на торговлю?

---

## 📊 Что обнаружено

### ✅ RSI **ИСПОЛЬЗУЕТСЯ** в стратегии

Индикатор RSI **критически важен** для стратегии Night Cat Samurai:

```json
"rules": {
  "long_entry": [
    {"left": "rsi", "op": "<", "right": 22}  ← Вход в лонг при RSI < 22
  ],

  "short_entry": [
    {"left": "rsi", "op": ">", "right": 80}  ← Вход в шорт при RSI > 80
  ],

  "long_exit": [
    {"left": "rsi", "op": ">", "right": 70}  ← Выход из лонга при RSI > 70
  ],

  "short_exit": [
    {"left": "rsi", "op": "<", "right": 30}  ← Выход из шорта при RSI < 30
  ]
}
```

**Вывод:** RSI используется **в 4 из 4 правил**! Это основной индикатор стратегии.

---

### ❌ Но параметры `rsi_ovs` и `rsi_ovb` **НЕ ИСПОЛЬЗУЮТСЯ**

#### Проблема:

В конфиге стратегии:
```json
{
  "rsi_len": 14,          ← ИСПОЛЬЗУЕТСЯ (период расчёта RSI)
  // "rsi_ovs": ???       ← НЕТ В КОНФИГЕ!
  // "rsi_ovb": ???       ← НЕТ В КОНФИГЕ!
}
```

**В правилах используются hardcoded значения:**
- Long entry: `rsi < 22` (не `rsi < rsi_ovs`)
- Short entry: `rsi > 80` (не `rsi > rsi_ovb`)
- Long exit: `rsi > 70`
- Short exit: `rsi < 30`

---

## 🤔 Почему возникает ошибка валидации?

### Текущая ситуация:

1. **UI форма** запрашивает `rsi_ovs` и `rsi_ovb` (значения по умолчанию: 30 и 70)
2. **Валидация** требует `rsi_ovs < rsi_ovb`
3. Вы ввели `ovs=36, ovb=32` → **ОШИБКА**
4. **НО:** Стратегия эти параметры **не использует**!

### Откуда берутся значения по умолчанию?

```python
# configs.py:25-26
rsi_ovb: int = 70  # RSI overbought threshold
rsi_ovs: int = 30  # RSI oversold threshold
```

Эти параметры **есть в IndicatorStrategyConfig**, но **не обязательны для использования**.

---

## 🎯 Два способа использования RSI

### Способ 1: Через параметры `rsi_ovs`/`rsi_ovb` ✅ (рекомендуется)

**Правила:**
```json
{
  "long_entry": [
    {"left": "rsi", "op": "<", "right": "rsi_ovs"}  ← Использует параметр
  ],

  "short_entry": [
    {"left": "rsi", "op": ">", "right": "rsi_ovb"}  ← Использует параметр
  ]
}
```

**Плюсы:**
- ✅ Можно менять пороги RSI через UI без изменения правил
- ✅ Валидация `ovs < ovb` имеет смысл
- ✅ Параметры влияют на торговлю

**Минусы:**
- ❌ Ограничены двумя порогами (ovs и ovb)

---

### Способ 2: Через конкретные значения в правилах ❌ (текущий)

**Правила:**
```json
{
  "long_entry": [
    {"left": "rsi", "op": "<", "right": 22}  ← Hardcoded значение
  ],

  "short_entry": [
    {"left": "rsi", "op": ">", "right": 80}  ← Hardcoded значение
  ]
}
```

**Плюсы:**
- ✅ Можно использовать разные пороги для entry/exit
- ✅ Более гибкие правила (22, 30, 70, 80)

**Минусы:**
- ❌ Параметры `rsi_ovs` и `rsi_ovb` **не влияют** на торговлю
- ❌ Валидация `ovs < ovb` бессмысленна
- ❌ Чтобы изменить пороги, нужно менять JSON правил

---

## 🔧 Что происходит в вашей стратегии

### Текущий конфиг:
```json
{
  "rsi_len": 14,  ← ВЛИЯЕТ (период RSI)

  // rsi_ovs и rsi_ovb отсутствуют в конфиге

  "rules": {
    "long_entry": [
      {"left": "rsi", "op": "<", "right": 22}   ← HARDCODED
    ],
    "short_entry": [
      {"left": "rsi", "op": ">", "right": 80}   ← HARDCODED
    ],
    "long_exit": [
      {"left": "rsi", "op": ">", "right": 70}   ← HARDCODED
    ],
    "short_exit": [
      {"left": "rsi", "op": "<", "right": 30}   ← HARDCODED
    ]
  }
}
```

### Что влияет на торговлю:

| Параметр | Влияет? | Где используется |
|----------|---------|------------------|
| `rsi_len` | ✅ ДА | Период расчёта RSI (14 баров) |
| `rsi_ovs` | ❌ НЕТ | Нигде (есть в UI, но не в rules) |
| `rsi_ovb` | ❌ НЕТ | Нигде (есть в UI, но не в rules) |
| Значения в rules (22, 30, 70, 80) | ✅ ДА | Условия входа/выхода |

---

## 💡 Рекомендации

### Вариант 1: Игнорировать валидацию `rsi_ovs`/`rsi_ovb` (быстрое решение)

Поскольку ваша стратегия не использует эти параметры:
1. Просто установите корректные значения: `ovs=30, ovb=70`
2. Эти значения **не повлияют** на торговлю
3. Валидация пройдёт успешно

**Команда:**
```json
{
  "rsi_ovs": 30,  // Любое значение < 70
  "rsi_ovb": 70   // Любое значение > 30
}
```

---

### Вариант 2: Использовать параметры в правилах (улучшение стратегии)

Измените правила чтобы использовать параметры:

**БЫЛО:**
```json
{
  "long_entry": [
    {"left": "rsi", "op": "<", "right": 22}
  ]
}
```

**СТАЛО:**
```json
{
  "rsi_ovs": 22,  // Добавить в конфиг

  "long_entry": [
    {"left": "rsi", "op": "<", "right": "rsi_ovs"}  // Использовать параметр
  ]
}
```

**Плюсы:**
- ✅ Можно менять пороги через UI
- ✅ Валидация имеет смысл

**Минусы:**
- ❌ Придётся использовать те же пороги для всех условий с ovs/ovb

---

### Вариант 3: Отключить валидацию для неиспользуемых параметров

Улучшить валидацию чтобы проверять только те индикаторы, которые используются в правилах.

**Псевдокод:**
```python
# Проверить используется ли rsi_ovs или rsi_ovb в правилах
rules = config.get("rules", {})
uses_rsi_params = False

for rule_list in rules.values():
    for rule in rule_list:
        if rule.get("right") in ["rsi_ovs", "rsi_ovb"]:
            uses_rsi_params = True
            break

# Валидировать только если используется
if uses_rsi_params and rsi_ovs >= rsi_ovb:
    raise HTTPException(...)
```

---

## 📝 Ответ на ваш вопрос

### **Q: Проверь, этот индикатор вообще влияет на стратегию?**

**A:**

1. **RSI индикатор** → ✅ **ДА, влияет критически!**
   - Используется во всех 4 правилах (long_entry, short_entry, long_exit, short_exit)
   - Основной сигнал стратегии

2. **Параметр `rsi_len`** → ✅ **ДА, влияет!**
   - Определяет период расчёта RSI (14 баров)

3. **Параметры `rsi_ovs` и `rsi_ovb`** → ❌ **НЕТ, НЕ влияют!**
   - Присутствуют в UI форме
   - **НЕ используются** в правилах вашей стратегии
   - Правила используют hardcoded значения: 22, 30, 70, 80
   - Можете установить любые значения (соблюдая ovs < ovb) - на торговлю не повлияет

---

## 🎯 Практический совет

**Для вашей стратегии `night_cat_samurai_strategy`:**

Просто установите:
- `rsi_ovs = 30` (или любое < 70)
- `rsi_ovb = 70` (или любое > 30)

Это **не изменит** торговую логику, так как стратегия использует значения из rules (22, 30, 70, 80).

**Если хотите реально изменить пороги RSI** → измените значения **в правилах (rules)**, а не в параметрах ovs/ovb.

---

## 📊 Таблица влияния параметров

| Параметр | В конфиге? | В правилах? | Влияет на торговлю? | Как изменить? |
|----------|------------|-------------|---------------------|---------------|
| `rsi_len` | ✅ Да (14) | ✅ Используется для расчёта | ✅ ДА | Изменить в UI |
| `rsi_ovs` | ❌ Нет | ❌ Не используется (hardcoded 22, 30) | ❌ НЕТ | Бесполезно менять |
| `rsi_ovb` | ❌ Нет | ❌ Не используется (hardcoded 70, 80) | ❌ НЕТ | Бесполезно менять |
| Значения в rules | - | ✅ 22, 30, 70, 80 | ✅ ДА | Изменить JSON правил |

---

## ✅ Итоговая рекомендация

1. **Для прохождения валидации:** Установите `rsi_ovs=30, rsi_ovb=70`
2. **Для реального изменения логики RSI:** Измените значения в `rules` (строки 34, 42, 48, 52)
3. **Для будущих стратегий:** Используйте параметры `rsi_ovs`/`rsi_ovb` в правилах как `"right": "rsi_ovs"` вместо hardcoded чисел

---

**Спасибо за отличный вопрос!** Это выявило важную проблему в валидации - мы проверяем параметры, которые могут не использоваться в конкретной стратегии.
