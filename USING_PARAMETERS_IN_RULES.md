# 🎯 Как использовать параметры вместо hardcoded значений в правилах

**Дата:** 2025-11-21
**Стратегия:** night_cat_samurai_strategy (пример)

---

## 📊 Что было изменено

### БЫЛО (hardcoded значения):

```json
{
  "rsi_len": 14,
  // rsi_ovs и rsi_ovb отсутствуют

  "rules": {
    "long_entry": [
      {"left": "hour", "op": ">=", "right": 0},          ← Hardcoded
      {"left": "hour", "op": "<=", "right": 7},          ← Hardcoded
      {"left": "rsi",  "op": "<",  "right": 22}          ← Hardcoded
    ],

    "short_entry": [
      {"left": "rsi",  "op": ">",  "right": 80}          ← Hardcoded
    ],

    "long_exit": [
      {"left": "rsi", "op": ">", "right": 70}            ← Hardcoded
    ],

    "short_exit": [
      {"left": "rsi", "op": "<", "right": 30}            ← Hardcoded
    ]
  }
}
```

**Проблемы:**
- ❌ Нельзя изменить пороги RSI через UI
- ❌ Нельзя изменить часы time_filter через UI
- ❌ Параметры `rsi_ovs`, `rsi_ovb` не влияют на торговлю
- ❌ Нужно редактировать JSON правил для изменения порогов

---

### СТАЛО (используются параметры):

```json
{
  "rsi_len": 14,
  "rsi_ovs": 22,                                         ← Добавлен параметр
  "rsi_ovb": 80,                                         ← Добавлен параметр

  "time_filter_enabled": true,
  "time_filter_start_hour": 0,
  "time_filter_end_hour": 7,

  "rules": {
    "long_entry": [
      {"left": "hour", "op": ">=", "right": "time_filter_start_hour"},  ← Параметр
      {"left": "hour", "op": "<=", "right": "time_filter_end_hour"},    ← Параметр
      {"left": "rsi",  "op": "<",  "right": "rsi_ovs"}                  ← Параметр
    ],

    "short_entry": [
      {"left": "hour", "op": ">=", "right": "time_filter_start_hour"},  ← Параметр
      {"left": "hour", "op": "<=", "right": "time_filter_end_hour"},    ← Параметр
      {"left": "rsi",  "op": ">",  "right": "rsi_ovb"}                  ← Параметр
    ],

    "long_exit": [
      {"left": "rsi", "op": ">", "right": "rsi_ovb"}                    ← Параметр
    ],

    "short_exit": [
      {"left": "rsi", "op": "<", "right": "rsi_ovs"}                    ← Параметр
    ]
  }
}
```

**Преимущества:**
- ✅ Можно изменять пороги RSI через UI
- ✅ Можно изменять часы торговли через UI
- ✅ Параметры `rsi_ovs`, `rsi_ovb` **реально влияют** на торговлю
- ✅ Валидация `ovs < ovb` имеет смысл
- ✅ Не нужно редактировать JSON правил

---

## 🔧 Как это работает

### 1. Добавляем параметры в конфиг

```json
{
  "rsi_ovs": 22,   // RSI порог для перепроданности (вход в лонг)
  "rsi_ovb": 80    // RSI порог для перекупленности (вход в шорт)
}
```

### 2. Используем параметры в правилах (строки в кавычках)

```json
{
  "left": "rsi",
  "op": "<",
  "right": "rsi_ovs"   // ← Строка = ссылка на параметр
}
```

**Важно:** Параметр указывается как **строка** (в кавычках), а не число!

---

## 📋 Таблица изменений

| Правило | БЫЛО | СТАЛО | Что изменяется через UI |
|---------|------|-------|-------------------------|
| **long_entry hour >=** | `0` (число) | `"time_filter_start_hour"` (параметр) | Start Hour (UTC) |
| **long_entry hour <=** | `7` (число) | `"time_filter_end_hour"` (параметр) | End Hour (UTC) |
| **long_entry rsi <** | `22` (число) | `"rsi_ovs"` (параметр) | RSI Oversold |
| **short_entry hour >=** | `0` (число) | `"time_filter_start_hour"` (параметр) | Start Hour (UTC) |
| **short_entry hour <=** | `7` (число) | `"time_filter_end_hour"` (параметр) | End Hour (UTC) |
| **short_entry rsi >** | `80` (число) | `"rsi_ovb"` (параметр) | RSI Overbought |
| **long_exit rsi >** | `70` (число) | `"rsi_ovb"` (параметр) | RSI Overbought |
| **short_exit rsi <** | `30` (число) | `"rsi_ovs"` (параметр) | RSI Oversold |

---

## 💡 Примеры использования параметров

### Доступные параметры для использования в правилах:

#### **RSI параметры:**
```json
"rsi_ovs"        // RSI oversold threshold (например: 22, 30)
"rsi_ovb"        // RSI overbought threshold (например: 70, 80)
```

#### **Time Filter параметры:**
```json
"time_filter_start_hour"   // Start hour UTC (0-23)
"time_filter_end_hour"     // End hour UTC (0-23)
```

#### **EMA параметры:**
```json
"ema_fast_len"   // EMA fast period
"ema_slow_len"   // EMA slow period
```

#### **Другие индикаторы (уже используются как параметры):**
```json
"lower_bb"       // Bollinger Bands lower band (автоматически вычисляется)
"upper_bb"       // Bollinger Bands upper band
"middle_bb"      // Bollinger Bands middle band
"ema_fast"       // Fast EMA value
"ema_slow"       // Slow EMA value
"rsi"            // RSI value
"atr"            // ATR value
"adx"            // ADX value
"vol_threshold"  // Volume threshold (vol_ma * vol_mult)
```

---

## 🎯 Как изменять параметры

### Через UI (теперь работает!):

1. Откройте стратегию: `/ui/strategies/night_cat_samurai_strategy/edit`
2. Измените параметры:
   - **RSI Oversold:** 22 → 25 (менее агрессивный вход)
   - **RSI Overbought:** 80 → 75 (менее агрессивный вход)
   - **Start Hour (UTC):** 0 → 1 (начало с 1:00)
   - **End Hour (UTC):** 7 → 8 (окончание в 8:00)
3. Нажмите **"Recalculate"** → увидите новые результаты
4. Нажмите **"Save Strategy"** → параметры сохранятся

**Теперь изменения РЕАЛЬНО влияют на торговлю!** 🎉

---

## 📊 Влияние изменений параметров

### Пример 1: Изменение RSI Oversold (22 → 30)

**БЫЛО:**
```json
{"left": "rsi", "op": "<", "right": "rsi_ovs"}  // rsi_ovs = 22
```
- Вход в лонг при RSI < 22 (очень перепродано)
- Меньше сигналов, но более сильные

**СТАЛО:**
```json
{"left": "rsi", "op": "<", "right": "rsi_ovs"}  // rsi_ovs = 30
```
- Вход в лонг при RSI < 30 (умеренно перепродано)
- Больше сигналов, но менее сильные

---

### Пример 2: Изменение Time Filter (0-7 → 1-8)

**БЫЛО:**
```json
{"left": "hour", "op": ">=", "right": "time_filter_start_hour"}  // = 0
{"left": "hour", "op": "<=", "right": "time_filter_end_hour"}    // = 7
```
- Торговля с 00:00 до 07:00 UTC (8 часов)

**СТАЛО:**
```json
{"left": "hour", "op": ">=", "right": "time_filter_start_hour"}  // = 1
{"left": "hour", "op": "<=", "right": "time_filter_end_hour"}    // = 8
```
- Торговля с 01:00 до 08:00 UTC (8 часов, сдвиг на час)

---

## ⚠️ Важные замечания

### 1. Параметры vs Числа в правилах

**Параметр (строка):**
```json
{"left": "rsi", "op": "<", "right": "rsi_ovs"}
```
- ✅ Значение берется из конфига
- ✅ Можно изменять через UI
- ✅ Валидация работает

**Число:**
```json
{"left": "rsi", "op": "<", "right": 22}
```
- ❌ Фиксированное значение
- ❌ Нельзя изменить через UI
- ❌ Валидация параметров бесполезна

---

### 2. Валидация теперь имеет смысл

Поскольку правила используют `rsi_ovs` и `rsi_ovb`:

```
rsi_ovs < rsi_ovb  ← КРИТИЧНО!
```

**Если нарушить:**
- long_exit: `rsi > rsi_ovb` (например, 80)
- short_exit: `rsi < rsi_ovs` (например, 22)

Это корректно! Выходим из лонга при RSI > 80, из шорта при RSI < 22.

---

### 3. Использование одного параметра в разных правилах

**Умная стратегия:**
```json
{
  "rsi_ovs": 22,
  "rsi_ovb": 80,

  "long_entry": [
    {"left": "rsi", "op": "<", "right": "rsi_ovs"}   // Вход при RSI < 22
  ],

  "short_exit": [
    {"left": "rsi", "op": "<", "right": "rsi_ovs"}   // Выход при RSI < 22
  ]
}
```

Изменяя `rsi_ovs` в UI (например, 22 → 25):
- ✅ Вход в лонг станет менее агрессивным (RSI < 25)
- ✅ Выход из шорта также изменится (RSI < 25)
- ✅ Параметры согласованы

---

## 🚀 Полный пример стратегии с параметрами

```json
{
  "strategy_type": "indicator",
  "mode": "quant_only",
  "symbol": "ETHUSDT",

  "allow_long": true,
  "allow_short": true,

  "base_position_pct": 10.0,
  "pyramiding": 4,
  "use_martingale": true,
  "martingale_mult": 1.5,

  "tp_long_pct": 2.0,
  "sl_long_pct": 2.0,
  "tp_short_pct": 2.0,
  "sl_short_pct": 2.0,
  "use_tp_sl": true,

  "rsi_len": 14,
  "rsi_ovs": 22,
  "rsi_ovb": 80,

  "bb_len": 20,
  "bb_mult": 2.0,
  "vol_ma_len": 21,
  "vol_mult": 0.5,

  "time_filter_enabled": true,
  "time_filter_start_hour": 0,
  "time_filter_end_hour": 7,

  "rules": {
    "long_entry": [
      {"left": "hour", "op": ">=", "right": "time_filter_start_hour"},
      {"left": "hour", "op": "<=", "right": "time_filter_end_hour"},
      {"left": "rsi",  "op": "<",  "right": "rsi_ovs"},
      {"left": "low",  "op": "<=", "right": "lower_bb"},
      {"left": "volume", "op": ">", "right": "vol_threshold"}
    ],

    "short_entry": [
      {"left": "hour", "op": ">=", "right": "time_filter_start_hour"},
      {"left": "hour", "op": "<=", "right": "time_filter_end_hour"},
      {"left": "rsi",  "op": ">",  "right": "rsi_ovb"},
      {"left": "high", "op": ">=", "right": "upper_bb"},
      {"left": "volume", "op": ">", "right": "vol_threshold"}
    ],

    "long_exit": [
      {"left": "rsi", "op": ">", "right": "rsi_ovb"}
    ],

    "short_exit": [
      {"left": "rsi", "op": "<", "right": "rsi_ovs"}
    ]
  }
}
```

**Все параметры теперь управляются через UI!** ✅

---

## 📚 Сводная таблица: Hardcoded vs Параметры

| Аспект | Hardcoded числа | Параметры (строки) |
|--------|-----------------|-------------------|
| **Пример** | `"right": 22` | `"right": "rsi_ovs"` |
| **Редактирование** | Только JSON | Через UI |
| **Валидация** | Не применима | Работает |
| **Гибкость** | Низкая | Высокая |
| **Тестирование** | Нужно менять файл | Быстрый пересчет |
| **Ошибки** | Легко ошибиться | Валидация защищает |
| **Рекомендуется** | Для фиксированных значений | **Для настраиваемых параметров** ✅ |

---

## ✅ Итоговые рекомендации

### Когда использовать параметры:

✅ **RSI пороги** (`rsi_ovs`, `rsi_ovb`) - часто меняются
✅ **Time filter** (`time_filter_start_hour`, `time_filter_end_hour`) - для тестирования разных сессий
✅ **Любые значения, которые вы хотите оптимизировать**

### Когда использовать hardcoded числа:

❌ **Фиксированные константы** (например, количество баров для lookback)
❌ **Значения, которые никогда не меняются**
❌ **Промежуточные расчеты**

---

## 🎓 Пример оптимизации стратегии

**Workflow:**
1. Открыть стратегию в UI
2. Изменить `rsi_ovs`: 22 → 25 → 30 (тестируем разные пороги)
3. Нажать **Recalculate** после каждого изменения
4. Сравнить результаты (PnL, Win Rate, Drawdown)
5. Выбрать лучшие параметры
6. Нажать **Save Strategy**

**Без параметров:** Пришлось бы редактировать JSON файл 3 раза и перезапускать бэктест вручную.

**С параметрами:** Меняем в UI, мгновенный пересчет, сохраняем лучший вариант. 🚀

---

**Готово!** Теперь ваша стратегия полностью управляется через UI параметры.
