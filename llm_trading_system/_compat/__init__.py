"""Compatibility helpers for optional dependencies."""

# Currently intentionally empty; modules inside this package are imported for
# their side effects (for example, the lightweight httpx stub that enables the
# Starlette/FastAPI TestClient to function when the real dependency cannot be
# installed in the execution environment).

