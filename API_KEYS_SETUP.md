# Настройка API ключей

Есть **3 способа** передать API ключи в тест.

## ✅ Способ 1: Аргументы командной строки (Рекомендуется)

Самый простой и надежный способ:

```bash
python3 test_full_cycle.py --real-data \
  --cryptopanic-key "your_cryptopanic_key" \
  --newsapi-key "your_newsapi_key" \
  --model gpt-oss:20b
```

**Преимущества:**
- Не нужно настраивать окружение
- Ключи передаются напрямую
- Работает всегда

## ✅ Способ 2: Переменные окружения (в той же сессии)

Установите переменные **В ТОМ ЖЕ ТЕРМИНАЛЕ** где запускаете тест:

```bash
# Установить переменные
export CRYPTOPANIC_API_KEY="your_cryptopanic_key"
export NEWSAPI_KEY="your_newsapi_key"

# Проверить что они установлены
python3 check_env.py

# Запустить тест (в том же терминале!)
python3 test_full_cycle.py --real-data
```

**Важно:** Переменные действуют только в текущей сессии терминала!

## ✅ Способ 3: Файл .env

Создайте файл `.env` из шаблона:

```bash
# Скопируйте пример
cp .env.example .env

# Отредактируйте .env, добавьте свои ключи
nano .env  # или vim, или другой редактор
```

Содержимое `.env`:
```bash
export CRYPTOPANIC_API_KEY="your_real_key_here"
export NEWSAPI_KEY="your_real_key_here"
export BASE_ASSET="BTCUSDT"
export HORIZON_HOURS="4"
```

Загрузите переменные и запустите тест:

```bash
# Загрузить переменные из .env
source .env

# Проверить
python3 check_env.py

# Запустить тест
python3 test_full_cycle.py --real-data
```

## 🔍 Проверка настройки

Проверьте что ключи видны скрипту:

```bash
python3 check_env.py
```

Вы должны увидеть:
```
✓ CRYPTOPANIC_API_KEY: abc1***xyz9
✓ NEWSAPI_KEY: def2***uvw8
```

Если видите `✗ NOT SET` - ключи не установлены!

## ❌ Частые ошибки

### Ошибка: "API key missing" хотя ключ установлен

**Причина:** Переменная установлена в другом терминале

**Решение:** Выполните `export` в том же терминале где запускаете тест

### Ошибка: Переменная не экспортирована

```bash
# ❌ НЕПРАВИЛЬНО (без export)
CRYPTOPANIC_API_KEY="key"

# ✅ ПРАВИЛЬНО (с export)
export CRYPTOPANIC_API_KEY="key"
```

### Ошибка: source .env не работает

**Причина:** Файл .env не содержит `export`

**Решение:** В файле .env обязательно должно быть:
```bash
export CRYPTOPANIC_API_KEY="..."  # export обязателен!
```

## 📝 Пример полной сессии

```bash
# Терминал 1: Запустите Ollama
ollama serve

# Терминал 2: Запустите тест
cd /home/user/LLM_traiding_system

# Вариант А: Через аргументы (проще всего)
python3 test_full_cycle.py --real-data \
  --cryptopanic-key "abc123xyz" \
  --newsapi-key "def456uvw" \
  --model gpt-oss:20b

# Вариант Б: Через переменные окружения
export CRYPTOPANIC_API_KEY="abc123xyz"
export NEWSAPI_KEY="def456uvw"
python3 check_env.py  # Проверка
python3 test_full_cycle.py --real-data --model gpt-oss:20b

# Вариант В: Через .env файл
nano .env  # Добавьте ключи
source .env
python3 test_full_cycle.py --real-data --model gpt-oss:20b
```

## 🎯 Рекомендация

Для разового теста используйте **Способ 1** (аргументы командной строки).

Для постоянной работы создайте файл `.env` (**Способ 3**).

## ℹ️ Без API ключей

Если у вас нет API ключей, тест все равно работает:

```bash
# Использует mock данные (без реальных API)
python3 test_full_cycle.py
```

Все компоненты тестируются, только данные будут тестовые.

## 🔑 Получение API ключей

- **CryptoPanic**: https://cryptopanic.com/developers/api/
- **NewsAPI**: https://newsapi.org/register

Оба сервиса имеют бесплатные планы.
