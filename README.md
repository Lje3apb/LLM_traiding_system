# LLM Trading System - Торговая система на основе LLM

Система для автоматического расчёта размеров позиций на основе режимов рынка, определённых с помощью LLM (Large Language Model).

## 🚀 Возможности

- **Position Sizing Module** - Расчёт множителей позиций на основе вероятностей режимов и рыночных показателей
- **Market Snapshot** - Сбор данных с бирж, on-chain метрик и новостей
- **Бесплатные источники данных** - Использует только бесплатные API без необходимости подписки:
  - CoinMetrics Community API - on-chain метрики BTC и стейблкоинов
  - Blockchain.com Charts API - адресная активность (fallback)
  - Binance API - рыночные данные и цены
  - CryptoPanic API (опционально) - новости криптовалют
  - NewsAPI (опционально) - дополнительные новости
- **Режимная торговля** - Адаптация размеров позиций к текущему режиму рынка (bull/bear/neutral)
- **Управление рисками** - Автоматическое снижение позиций при высоких рисках
- **Docker Ready** - Полная контейнеризация для лёгкого развёртывания

## 📋 Требования

- Docker >= 20.10
- Docker Compose >= 2.0
- (Опционально) Python 3.12+ для локального запуска

## 🔧 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/Lje3apb/LLM_traiding_system.git
cd LLM_traiding_system
```

### 2. Настройка переменных окружения

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать .env файл (опционально добавить API ключи)
nano .env  # или используйте любой другой редактор
```

### 3. Запуск с помощью Docker

#### Вариант A: Запуск модуля расчёта позиций (Position Sizing)

```bash
# Собрать и запустить контейнер
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d
```

#### Вариант B: Запуск тестов

```bash
# Запустить все unit-тесты
docker-compose --profile test up test

# Посмотреть результаты
docker-compose logs test
```

#### Вариант C: Запуск сбора рыночных данных

```bash
# Запустить market snapshot (требуются API ключи в .env)
docker-compose --profile market up market-snapshot
```

### 4. Локальный запуск без Docker

```bash
# Создать виртуальное окружение
python3.12 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить примеры
python position_sizing.py

# Запустить тесты
python -m unittest test_position_sizing.py -v

# Запустить market snapshot (требуются API ключи)
python market_snapshot.py
```

## 📊 Проверка работы системы

### Проверка 1: Модуль Position Sizing

```bash
# Запуск примеров с демонстрацией работы
docker-compose up

# Ожидаемый вывод:
# - Example 1: Bullish regime
# - Example 2: Bearish regime
# - Example 3: Neutral regime
# - Example 4: High risk environment
# - Example 5: Extreme risk
```

**Что проверяем:**
- ✅ Множители k_long и k_short корректно рассчитываются
- ✅ В бычьем режиме k_long > 1.0, k_short < 1.0
- ✅ В медвежьем режиме k_short > 1.0, k_long < 1.0
- ✅ При высоких рисках позиции уменьшаются
- ✅ При экстремальных рисках (>0.9) торговля отключается (k=0.0)

### Проверка 2: Unit-тесты

```bash
# Запуск всех тестов
docker-compose --profile test up test

# Ожидаемый результат: Ran 19 tests - OK
```

**Что тестируется:**
- ✅ Функции clamp и safe_get_score
- ✅ Обработка некорректных входных данных
- ✅ Нормализация вероятностей
- ✅ Влияние сентимента на множители
- ✅ Влияние on-chain данных
- ✅ Влияние силы тренда
- ✅ Снижение позиций при рисках
- ✅ Ограничение множителей k_max

### Проверка 3: Интеграционный тест

```python
# Создайте файл test_integration.py
cat > test_integration.py << 'EOF'
from position_sizing import compute_position_multipliers

# Тестовый LLM output
llm_output = {
    "prob_bull": 0.65,
    "prob_bear": 0.35,
    "scores": {
        "btc_sentiment": 0.4,
        "onchain_pressure": 0.2,
        "trend_strength": 0.7,
        "liquidity_risk": 0.3,
        "news_risk": 0.2,
    }
}

# Расчёт
pos_size, k_long, k_short = compute_position_multipliers(
    llm_output,
    side="long",
    base_long_size=0.01,
    base_short_size=0.01,
    k_max=2.0
)

print(f"Position size: {pos_size:.6f}")
print(f"Long multiplier: {k_long:.4f}")
print(f"Short multiplier: {k_short:.4f}")

# Проверки
assert k_long > 1.0, "В бычьем режиме k_long должен быть > 1.0"
assert k_short < 1.0, "В бычьем режиме k_short должен быть < 1.0"
assert pos_size == 0.01 * k_long, "Position size должен равняться base_size * k"
assert 0 <= k_long <= 2.0, "k_long должен быть в пределах [0, k_max]"

print("✅ Все проверки пройдены!")
EOF

# Запустить тест
docker-compose run --rm llm-trading python test_integration.py
```

### Проверка 4: API доступность (для market_snapshot)

```bash
# Проверка доступности Binance API
curl -s https://api.binance.com/api/v3/ticker/24hr?symbol=BTCUSDT | head -n 5

# Проверка доступности CoinMetrics Community API
curl -s "https://community-api.coinmetrics.io/v4/timeseries/asset-metrics?assets=btc&metrics=AdrActCnt&limit_per_asset=1" | head -n 10

# Проверка доступности Blockchain.com Charts API
curl -s "https://api.blockchain.info/charts/n-unique-addresses?timespan=1days&format=json" | head -n 10

# Все API должны возвращать JSON с данными
# CoinMetrics и Blockchain.com - полностью бесплатные, без ключей
```

## 🏗️ Архитектура

```
LLM_traiding_system/
├── position_sizing.py          # Основной модуль расчёта позиций
├── market_snapshot.py          # Сбор рыночных данных
├── test_position_sizing.py     # Unit-тесты
├── Dockerfile                  # Образ Docker
├── docker-compose.yml          # Оркестрация контейнеров
├── requirements.txt            # Python зависимости
├── .env.example               # Пример конфигурации
├── .gitignore                 # Игнорируемые файлы
└── README.md                  # Документация
```

## 📚 Использование API

### Основная функция: `compute_position_multipliers`

```python
from position_sizing import compute_position_multipliers

llm_output = {
    "prob_bull": 0.65,      # Вероятность бычьего режима [0, 1]
    "prob_bear": 0.35,      # Вероятность медвежьего режима [0, 1]
    "scores": {
        "global_sentiment": 0.3,     # Глобальный сентимент [-1, 1]
        "btc_sentiment": 0.4,        # BTC сентимент [-1, 1]
        "altcoin_sentiment": 0.2,    # Altcoin сентимент [-1, 1]
        "onchain_pressure": 0.2,     # On-chain давление [-1, 1]
        "liquidity_risk": 0.3,       # Риск ликвидности [0, 1]
        "news_risk": 0.2,            # Новостной риск [0, 1]
        "trend_strength": 0.7,       # Сила тренда [0, 1]
    }
}

# Расчёт для длинной позиции
position_size, k_long, k_short = compute_position_multipliers(
    llm_output=llm_output,
    side="long",                  # "long" или "short"
    base_long_size=0.01,         # 1% капитала на long
    base_short_size=0.01,        # 1% капитала на short
    k_max=2.0                    # Максимальный множитель
)

print(f"Размер позиции: {position_size:.6f}")  # = base_long_size * k_long
print(f"Множитель для long: {k_long:.4f}")
print(f"Множитель для short: {k_short:.4f}")
```

### Параметры

**Входные данные:**
- `prob_bull` - вероятность бычьего режима (0..1)
- `prob_bear` - вероятность медвежьего режима (0..1), обычно = 1 - prob_bull
- `scores.btc_sentiment` - настроение по BTC (-1 негатив, 1 позитив)
- `scores.onchain_pressure` - давление on-chain (-1 продажи, 1 покупки)
- `scores.trend_strength` - сила тренда (0 нет, 1 сильный)
- `scores.liquidity_risk` - риск ликвидности (0 низкий, 1 высокий)
- `scores.news_risk` - новостной риск (0 спокойно, 1 опасно)

**Возвращаемые значения:**
- `position_size` - итоговый размер позиции для данной сделки
- `k_long` - множитель для длинных позиций [0, k_max]
- `k_short` - множитель для коротких позиций [0, k_max]

## 🔬 Логика расчёта

Модуль использует многофакторную модель:

1. **Directional Bias** - базовый множитель от вероятностей режима
   - Dead-zone фильтр (d0=0.1) для фильтрации шума
   - d_eff = (|D| - d0) / (1 - d0), где D = prob_bull - prob_bear

2. **Score Adjustments** - корректировки на основе показателей:
   - Sentiment boost: β_sent = 0.5 (до ±50% эффект)
   - On-chain factor: β_chain = 0.3 (до ±30%)
   - Trend alignment: β_trend = 0.7 (до ±70%)

3. **Risk Throttling** - снижение при рисках:
   - Liquidity factor: α_liq = 0.7
   - News factor: α_news = 0.7
   - Hard stop при risk > 0.9 (все позиции = 0)

4. **Final Multipliers**:
   ```
   k_long = clamp(k_dir_long × sent_long × chain_long × trend_long × risk_factor, 0, k_max)
   k_short = clamp(k_dir_short × sent_short × chain_short × trend_short × risk_factor, 0, k_max)
   ```

## 🛡️ Безопасность

- ✅ Контейнеры запускаются от непривилегированного пользователя
- ✅ `.env` файл в `.gitignore` (секреты не попадают в git)
- ✅ Валидация всех входных данных
- ✅ Автоматическое ограничение множителей (k_max)
- ✅ Hard stop при экстремальных рисках

## 📈 Примеры результатов

```
Bullish regime (prob_bull=0.65):
  k_long:  1.3619  → позиция увеличена на 36%
  k_short: 0.6144  → позиция уменьшена на 39%

Bearish regime (prob_bear=0.70):
  k_long:  0.5733  → позиция уменьшена на 43%
  k_short: 1.8540  → позиция увеличена на 85%

Neutral regime (prob_bull≈0.5):
  k_long:  0.7200  → небольшое снижение из-за умеренных рисков
  k_short: 0.7200  → симметрично

High risk (liquidity_risk=0.7, news_risk=0.6):
  k_long:  0.6973  → сильное снижение из-за рисков
  k_short: 0.4533  → сильное снижение

Extreme risk (news_risk=0.95):
  k_long:  0.0000  → торговля отключена
  k_short: 0.0000  → торговля отключена
```

## 🔄 Обновление

```bash
# Остановить контейнеры
docker-compose down

# Получить обновления
git pull origin main

# Пересобрать и запустить
docker-compose up --build
```

## 🐛 Troubleshooting

### Проблема: "Cannot connect to the Docker daemon"
```bash
# Запустить Docker daemon
sudo systemctl start docker
```

### Проблема: "Permission denied" при запуске
```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
# Перелогиниться
```

### Проблема: Тесты падают
```bash
# Проверить логи
docker-compose logs test

# Запустить интерактивно для отладки
docker-compose run --rm llm-trading python -m unittest test_position_sizing.py -v
```

### Проблема: API keys не работают
```bash
# Проверить, что .env файл существует и правильно заполнен
cat .env | grep API_KEY

# Проверить, что docker-compose подхватывает .env
docker-compose config
```

## 📝 Лицензия

См. файл [LICENSE](LICENSE)

## 🤝 Вклад в проект

Pull requests приветствуются! Для значительных изменений сначала откройте issue для обсуждения.

## 📧 Контакты

- GitHub: [@Lje3apb](https://github.com/Lje3apb)
- Issues: [GitHub Issues](https://github.com/Lje3apb/LLM_traiding_system/issues)

---

**⚠️ Disclaimer:** Эта система предназначена только для образовательных целей. Используйте на свой страх и риск. Автор не несёт ответственности за финансовые потери.
