# 🚀 Быстрый старт: Полный цикл тестирования

Пошаговая инструкция для запуска полного цикла торговой системы с LLM.

## ⚡ За 3 минуты

```bash
# 1. Проверьте зависимости
python3 check_dependencies.py

# 2. Убедитесь что Ollama запущена (если нет - запустите)
ollama serve  # в отдельном терминале

# 3. Загрузите модель (если еще нет)
ollama pull llama3.2

# 4. Запустите полный тест
python3 test_full_cycle.py
```

Готово! Вы увидите:
- ✅ Сбор данных
- ✅ Запрос к LLM
- ✅ Анализ рынка
- ✅ Торговое решение

## 📊 Что происходит в тесте

```
Данные → Промпты → LLM → Парсинг → Расчет → Решение
  ↓         ↓       ↓       ↓        ↓        ↓
Mock/API   Build  Ollama  JSON    k_long   LONG/SHORT
```

### Пример вывода

```
TRADING RECOMMENDATION:
FAVOR LONG positions (multiplier: 1.72x)

REGIME ASSESSMENT:
  Regime:      BULL
  Confidence:  MEDIUM
  Bull prob:   68.0%
  Bear prob:   32.0%

POSITION SIZING:
  → LONG position:    1.72% capital
  → SHORT position:   0.46% capital
```

## 🔧 Доступные команды

### Основные тесты

```bash
# Быстрый тест с mock данными
python3 test_full_cycle.py

# С реальными данными (требует API ключи)
python3 test_full_cycle.py --real-data

# С конкретной моделью
python3 test_full_cycle.py --model mistral
```

### Проверки

```bash
# Проверка зависимостей
python3 check_dependencies.py

# Проверка Ollama
./check_ollama.sh

# Быстрый тест Ollama API
python3 quick_test_ollama.py

# Тест llm_infra пакета
python3 test_ollama.py

# Тест position_sizing
python3 test_integration.py
```

## 📁 Структура тестов

| Файл | Назначение |
|------|------------|
| `test_full_cycle.py` | ⭐ **Главный тест** - весь цикл от начала до конца |
| `test_ollama.py` | Тест llm_infra пакета с Ollama |
| `quick_test_ollama.py` | Быстрая проверка Ollama API |
| `test_integration.py` | Тест модуля position_sizing |
| `check_dependencies.py` | Проверка зависимостей |
| `check_ollama.sh` | Проверка статуса Ollama |

## 🎯 Модели для тестирования

| Модель | Время | Качество | Команда |
|--------|-------|----------|---------|
| llama3.2 | ~15 сек | ⭐⭐⭐ | `ollama pull llama3.2` |
| mistral | ~30 сек | ⭐⭐⭐⭐ | `ollama pull mistral` |
| gpt-oss:20b | ~60 сек | ⭐⭐⭐⭐⭐ | `ollama pull gpt-oss:20b` |

## 🐛 Решение проблем

### "Connection refused"
```bash
# Запустите Ollama
ollama serve
```

### "Model not found"
```bash
# Загрузите модель
ollama pull llama3.2
```

### "Invalid JSON"
```bash
# Попробуйте другую модель (больше параметров = лучше)
python3 test_full_cycle.py --model mistral
```

### "Import Error"
```bash
# Установите зависимости
pip install -r requirements.txt
```

## 📚 Подробная документация

- [FULL_CYCLE_TEST.md](FULL_CYCLE_TEST.md) - полная документация теста
- [OLLAMA_SETUP.md](OLLAMA_SETUP.md) - настройка Ollama
- [example_usage.py](example_usage.py) - примеры использования llm_infra

## 🎓 Следующие шаги

После успешного теста:

1. **Попробуйте разные модели**
   ```bash
   python3 test_full_cycle.py --model mistral
   python3 test_full_cycle.py --model gpt-oss:20b
   ```

2. **Протестируйте с реальными данными**
   ```bash
   export CRYPTOPANIC_API_KEY="your_key"
   python3 test_full_cycle.py --real-data
   ```

3. **Интегрируйте в своего бота**
   - Используйте код из `test_full_cycle.py` как пример
   - Настройте автоматический запуск каждые N минут
   - Логируйте результаты для анализа

4. **Оптимизируйте параметры**
   - Подберите оптимальный `base_size`
   - Настройте `k_max` под свою стратегию
   - Отрегулируйте risk throttling

## 💡 Полезные ссылки

- [Ollama Models](https://ollama.com/library) - доступные модели
- [Ollama Documentation](https://github.com/ollama/ollama) - документация
- Project modules:
  - `llm_infra/` - LLM инфраструктура
  - `market_snapshot.py` - сбор данных
  - `position_sizing.py` - расчет позиций

---

**Нужна помощь?** Запустите `python3 check_dependencies.py` для диагностики
